# Shared dependencies for all three services (ride-api, matching-service, dispatch-service).
flask>=3.0,<4.0
requests>=2.31,<3.0
prometheus-client>=0.20,<1.0

# OpenTelemetry — tracing + OTLP export to Jaeger (Person 2 / observability layer).
opentelemetry-api>=1.24.0,<2.0
opentelemetry-sdk>=1.24.0,<2.0
opentelemetry-exporter-otlp-proto-http>=1.24.0,<2.0
opentelemetry-instrumentation-flask>=0.45b0
opentelemetry-instrumentation-requests>=0.45b0
